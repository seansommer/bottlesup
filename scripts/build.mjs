import{build}from'esbuild';import{cp,mkdir,rm}from'node:fs/promises';await rm('dist',{recursive:true,force:true});await mkdir('dist',{recursive:true});await build({entryPoints:['src/main.js'],bundle:true,format:'esm',minify:true,outfile:'dist/app.js',target:['es2022'],external:['https://*'],legalComments:'eof'});for(const file of ['index.html','styles.css','manifest.webmanifest','service-worker.js'])await cp(file,`dist/${file}`);await cp('assets','dist/assets',{recursive:true});console.log('Bottles Up! built.');
